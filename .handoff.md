# Handoff: 2026-08-31

## Goal

`github.com/dd0wney/fault` — SQLite's fault-injection loops as a Go library,
aimed at an eventual Go standard library proposal. That aim is LIVE, confirmed
by the user, which is why Windows CI is a blocking gate although the user
deploys nothing to Windows.

## State

`main` at `2b94d1e`, read from `git ls-remote origin main`. **Eight pull
requests merged today, #8 through #15. None open.** Linear history.

Six packages: `fault`, `fault/fs`, `fault/alloc`, `fault/role`, `fault/crash`,
and **`fault/sql`, which is new today**. 229 test functions. The dependency
gate reads 158 packages, standard library only — up from 108 this morning,
because it now reads test imports and because `fault/sql` pulls in
`database/sql`.

| Gate | State |
|---|---|
| mutation floors | `./` 0.77, `./fs/` 1.00, `./alloc/` 1.00, `./role/` 0.74, `./crash/` 0.77, `./sql/` 1.00 |
| coupling registry | 13 rows |
| dependency gate | root module AND `tools`, both standard library only |

**Every baseline row now carries a measured figure.** Four had words and no
number this morning.

## What landed today

- **#8 the dependency gate reads test imports.** `go list -deps` omits them,
  which is exactly where a driver import arrives. Also `--root`, the module
  path derived rather than hardcoded, and the `tools` module brought inside
  the gate.
- **#9 `crash.Recorder.Observed()`.** The paths the recorder actually served,
  so a caller can assert the sweep saw the file it is about.
- **#10 the `fault/sql` design**, six forks answered, plus a correction to the
  plan.
- **#11 #12 #13 `fault/sql` itself.** The seam, statements and transactions,
  rows, `doc.go` with the limits. Fork 3 is closed: the reflective test's
  `known` map is empty, so an entry in it is a regression.
- **#14 `mutation.sh` reads the package filter its usage documents.**
- **#15 each adapter proves it invents no failure**, plus a measured figure in
  every baseline row.

Outside the repository: the **graphdb mmap sweep harness now discriminates**.
See `/mnt/ssd2/Workspace/github.com/fault-graphdb-sweep/PLAN.md`, which carries
the numbers and the runbook.

## Two decisions waiting on the user

**`ResetSession` consumes no operation index.** It is the one forwarded
operation in `fault/sql` that does not count, and it is the opposite of the
rule `Begin` follows. The pool decides when to call it, so counting it would
make the N-th operation differ between two runs of one scenario, which is what
fork 2 protects. Documented in `sql/doc.go` and held by a test. Reversing it is
small.

**`./role/` has a floor of 0.74 and a measurement of 0.774194.** The house
method is the measured number rounded down, which is 0.77, and `./crash/`
raised itself from 0.75 to 0.77 on exactly that method. 0.74 leaves about two
survivors of slack, and slack of that kind is what hid the `./crash/` trend.
0.77 leaves less than one mutant, so one new survivor turns CI red. Raising a
floor is policy rather than measurement, so it was left and the disagreement
written into the row.

## Next

1. The graphdb harness against current `main`, not only the two fixed builds.
   Today's result is a regression test proving the harness works, not a live
   defect report.
2. `fault/sql` against a real driver in a scenario that is not a fixture.
3. The `./role/` floor decision above.

## Context to remember

**Twenty-five gaps the mutation gate found in `fault/sql` across three tasks,
and a reader found none of them.** The scores went 0.750000, 0.738095,
0.873016, each time to 1.000000 after repair. Two were worse than a missing
assertion.

**The invented-failure class.** `if r.armed && r.n == r.at` becoming
`if true && r.n == r.at` changed no test result, and that version fails the
first row of EVERY result set, armed or not. Every test in the suite armed
something, so none could see it. A missed injection under-reports and its
report stays true. An invented one reports a defect the code under test does
not have, and every later finding then needs a person to re-derive whether it
is real. `fs`, `alloc` and `role` now each carry a zero-`Points` guard against
it, written by hand because the gate cannot express the mutant where the
condition has one operand.

**A control that did not run looks exactly like a property that holds.** A
`sed` that matched nothing left `alloc` unmutated, the test passed, and that
reads identically to a real pass. Every control now prints a count proving the
mutation landed before the test runs.

**Grep for the change, not for the file you expect to hold it.**
`mmap_snapshot_writer.go` is byte-identical between graphdb's two builds, and
the durability fix is one directory away in `mmap_snapshot_persist.go`. A
conclusion was reported as definitive on the strength of that one file and had
to be retracted.

**Two publishes alone did not discriminate.** With the scoped rule disabled and
everything else identical, the broken build passed. Attribution by changing one
thing, not by changing several and crediting the likely one.

**CI found three things no local check could.** `golangci-lint` cannot run on
this machine — binary v2.12.2 built with go1.26.3 against a local go1.27.0
standard library — so CI is the only judge of it. It found SA1019 on
required-but-deprecated interface methods twice, and `errcheck` on one-value
type assertions twice. `.golangci.yml` sets `check-type-assertions`, so the
two-value form is required, and it is better anyway: it turns a panic into a
sentence.

**An empty result was the instrument four times today.** `gh run view --log`
refuses while a run is in progress. `gh api` withholds a log containing
terminal escape sequences unless `--allow-escape-sequences` is passed. A
`grep -c` finding nothing exits 1, which reads as a failed command. A `|| echo`
after a pipeline never fires, because the pipeline's status is the last stage's.
Each returned nothing and read exactly like a real absence.

**`go list -m` outside a module prints `command-line-arguments` and exits 0.**
It answers, and the answer is not a module path. `go env GOMOD` prints
`/dev/null` there, so it is the probe that can say no.

**A recorded number that ages is not a broken gate.** `./sql/` measured
1.000000 at 24 mutants, then 42, then 63. The score never moved, which is
exactly why each re-read was necessary: an identical figure about a larger
artifact is a different claim, and nothing in the gate can tell them apart.

**Do not check out commits in a shared working tree.** graphdb may be owned by
a peer session. Use an isolated clone, and `--no-hardlinks` when the
destination is on another filesystem.
